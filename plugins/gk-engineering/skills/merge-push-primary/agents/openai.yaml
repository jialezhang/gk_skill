interface:
  display_name: "Merge Push Primary"
  short_description: "Merge into main, commit, push, and delete the local source"
  default_prompt: "Use $merge-push-primary to merge the current branch’s changes into local main, then commit and push main, remain on main, and safely delete the original local branch."
